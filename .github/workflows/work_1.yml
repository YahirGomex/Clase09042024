name: Work 1 Yahir

on:
  push:
    branches:
      - test_1

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Run a multi-line script
      run: |
        echo "Impresión 1"
        echo "Impresión 2"
        echo "Impresión 3"
